@model IEnumerable<Diabetes1.Models.Activity>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.activity_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.activity_calories)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.activity_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.activity_calories)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_activity }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_activity }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_activity })
        </td>
    </tr>
}

</table>
